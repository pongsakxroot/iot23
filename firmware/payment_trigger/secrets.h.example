/*
 * Secrets Configuration Template
 * 
 * Copy this file to secrets.h and fill in your credentials
 * DO NOT commit secrets.h to version control!
 */

#ifndef SECRETS_H
#define SECRETS_H

// Wi-Fi Configuration
#define WIFI_SSID "Your-WiFi-SSID"
#define WIFI_PASSWORD "Your-WiFi-Password"

// MQTT Broker Configuration
#define MQTT_BROKER "192.168.1.100"    // IP address or hostname of MQTT broker
#define MQTT_PORT 1883                  // MQTT port (default: 1883)
#define MQTT_CLIENT_ID "esp32-payment"  // MQTT client ID (will be appended with MAC address)

// MQTT Authentication (leave empty if not required)
#define MQTT_USERNAME ""
#define MQTT_PASSWORD ""

// MQTT Topics
#define MQTT_TOPIC_TRIGGER "device/payment/trigger"

#endif // SECRETS_H
